platform :ios, '9.0'
use_frameworks!

target 'IBPresentation' do
  pod 'IBAnimatable', :git => 'https://github.com/IBAnimatable/IBAnimatable.git', :branch => 'master'
  pod 'Material', :git => 'https://github.com/CosmicMind/Material.git', :branch => 'development'
end

